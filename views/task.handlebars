{{!-- viewing the tasks/posts --}}
<header>
{{> nav}}
</header>
<body>


  

    {{{body}}}
    {{> alltaskstp}}
  
    {{!-- {{#each tasks}}
    <div class="task">
        <h3>{{user_name}}</h3>
        <p>{{user_task}}</p>

    </div>
    {{else}}
    <p>No tasks yet!</p>
    {{/each}} --}}

  </section>
</body>

{{> footer}}
</html>

<!-- RENDER TEMPLATE AFTER EVERYTHING ELSE LOADED -->
<script src="js/task.js"></script>